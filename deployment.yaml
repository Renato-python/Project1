apiVersion: apps/v1
kind: Deployment
metadata:
  name: abc_tech
  labels:
    app: abc_tech
spec: 
  replicas: 1
  selector:
    matchlabels:
       app: abc_tech   
  template:
    metadata:
      labels:
        app: abc_tech
    spec:
      containers:
        name: abc_tech
        image: renato71/abc_tech:latest
        ports:
          - containerPort: 80
---
apiVersion: v1
kind: Service
metadata: 
  name: abc-tech-service
spec:
  selector:
    app: abc_tech
  ports: 
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer           
